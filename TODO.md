# TODO: Add Active State Indicator to Navbar

- [x] Import usePathname from 'next/navigation' in src/components/Navbar.tsx
- [x] Add usePathname hook to get the current pathname
- [x] Modify desktop nav links to conditionally apply 'text-indigo-600 font-semibold' class if href matches pathname
- [x] Modify mobile nav links to conditionally apply 'text-indigo-600 font-semibold' class if href matches pathname
- [ ] Test the changes (run the app and navigate to different pages)
